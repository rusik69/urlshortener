apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "urlshortener.fullname" . }}-test-connection"
  labels:
    {{- include "urlshortener.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "urlshortener.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
